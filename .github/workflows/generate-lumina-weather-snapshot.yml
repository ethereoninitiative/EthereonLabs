name: Generate Lumina Weather Snapshot

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

permissions:
  contents: write

concurrency:
  group: lumina-weather-snapshot-${{ github.ref }}
  cancel-in-progress: false

jobs:
  generate-weather-snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate Lumina weather snapshot
        run: python LuminaOS/bootstrap/Ship_of_Ethereon_V2/runtime/generate_lumina_weather_snapshot_r1.py

      - name: Commit updated weather observation if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet -- data/lumina-weather-snapshot.json data/lumina-weather-history.json; then
            git add data/lumina-weather-snapshot.json data/lumina-weather-history.json
            git commit -m "Regenerate Lumina weather observation"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push
          else
            echo "Lumina weather observation unchanged."
          fi
